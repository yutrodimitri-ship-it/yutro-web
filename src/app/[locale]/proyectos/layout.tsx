import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Proyectos",
  description:
    "Explora nuestros proyectos de producción audiovisual con IA. Campañas creativas para marcas líderes con imágenes hiperrealistas y postproducción avanzada.",
};

export default function ProyectosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
