import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Servicios",
  description:
    "Servicios de producción audiovisual con IA: imágenes hiperrealistas, animación, 3D, motion graphics, postproducción y consistencia visual de marca.",
};

export default function ServiciosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
